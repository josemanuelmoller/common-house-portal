# Phase 4 & 5 Inventory — Notion-Write Routes & UI Gaps

**Generated:** 2026-05-05
**Scope:** Inventory for Phases 4 (API write migration) and 5 (UI gap fill) of the Supabase Consolidation Freeze.
**Source-of-truth doc:** `docs/SUPABASE_CONSOLIDATION_FREEZE.md`
**Cutoff:** 2026-06-02. All Notion writes after that date must go to Supabase.

---

## Section A — Phase 4: API routes (and lib helpers) that write to Notion

Every file below contains at least one call to `notion.pages.create`, `notion.pages.update`, `notion.databases.update`, or `notion.blocks.children.append`. Sorted by complexity descending (Complex → Trivial).

| # | File | Notion DB(s) written | Trigger | Supabase target (per freeze §3) | Complexity | Notes |
|---|---|---|---|---|---|---|
| 1 | `src/app/admin/decisions/actions.ts` | Decision Items (status), Organizations (create), arbitrary entities (relation/rich_text writes) | User (server actions from `/admin/decisions`) | `decision_items` (canonical, replacing `notion_decision_items` mirror); `organizations`; `people` (already partially Supabase) | **Complex** | 11 distinct write call sites. Critical migration: `approveDecision`, `resolveDecision`, `resolveWithNote`, `resolveWithSearch`, `resolveAndUpdateMulti`, `resolveWithRelationId`, `approveEntityCreation`, `approvePersonCreation`. People+org creation already partially writes to Supabase; the *Notion side* of those writes must be removed. Decision-item status writes must move to `decision_items` table directly. The "search-and-link relation" flows (`resolveWithSearch`, `previewRelationTarget`, `resolveWithRelationId`) need a Supabase equivalent: a relations table or FK update, not a Notion relation. **Migration note:** This is the largest single user-facing surface. Must be done as a unit so the `/admin/decisions` page does not regress mid-flight. Plan 1 PR per action group: (a) status/note writes → `decision_items`; (b) entity-creation writes → already Supabase, just delete the Notion fan-out; (c) relation-resolution flows → new Supabase semantics. |
| 2 | `src/app/api/garage-ingest/route.ts` | Evidence, Financial Snapshots, Valuations, Cap Table, Insight Briefs, Organizations (update), Sources, Knowledge Assets (via helper) | User (admin upload UI in `/admin/garage/[id]`) | `evidence`, `financial_snapshots`, `valuations`, `cap_table_entries`, `insight_briefs`, `organizations`, `sources`, `knowledge_assets` | **Complex** | 8+ Notion write call sites across 5 different DBs in one pipeline. PDF/Excel parse → AI extract → multi-DB fan-out. Some target tables (valuations, cap_table_entries, financial_snapshots, insight_briefs, knowledge_assets) are **NEW** Supabase tables that don't exist yet (Phase 1 dependency). **[migrated 2026-05-05]** All 10 write call sites now hit Supabase canonical tables. Notion calls retained as commented `notion-cutoff-2026-06-02` markers. Project-decision items now also written via `decision_items` (8th DB target, previously omitted from this row). The `notion.pages.retrieve(projectId)` read used to resolve the Primary Organization relation is the only remaining Notion call; flagged for removal once `projects` rows expose that relation as a typed column. `createKnowledgeAssetDraft()` helper in `src/lib/notion/knowledge.ts` is no longer called from this route — direct Supabase insert into `knowledge_assets` is used instead. |
| 3 | `src/app/api/clipper/route.ts` | Sources (web clip + WhatsApp create), Sources (update on enrichment) | User (Chrome extension Bearer token; admin session) | `sources` (already exists) + `conversation_messages` (already exists) | **Complex** | 3 write sites. Already partially writes to Supabase (`conversation_messages`, `sources` raw payload). Notion `sources` page creation is the leg to eliminate. Person resolver + project auto-link needs Supabase lookup. |
| 4 | `src/app/api/competitive-monitor/route.ts` | Competitive Intel (create), Watchlist (update last-seen) | Cron (`0 7 * * 1` weekly) + admin manual | `competitive_intel` (canonical, replacing `notion_competitive_intel` mirror); `watchlist_entities` | **Complex** | Anthropic web-search-based agent. Two DB targets, dry-run vs execute mode. Hall component already reads from `notion_competitive_intel` mirror — migration must keep that read path live. **[migrated 2026-05-05]** Both writes now hit Supabase canonical tables; Notion `pages.create` / `pages.update` retained as commented `notion-cutoff-2026-06-02` markers. Hall reader still on mirror until follow-up migration. |
| 5 | `src/app/api/scan-opportunity-candidates/route.ts` | Opportunities (create new candidates + update enriched candidates, update reviewed) | User (admin "Candidate scan" button) + CRON_SECRET | `opportunities` (already exists; add columns) | **Complex** | **Critical migration note:** 3 write call sites. Already reads sources from Supabase. Notion candidate creation is what feeds `/admin` Candidate Section. Must atomically migrate: create-in-supabase + remove Notion path + update read paths in `CandidateSection.tsx`/`HallScanCandidatesButton`. The de-dup logic against existing opportunities also runs on Notion today — needs a Supabase variant. |
| 6 | `src/app/api/promote-candidate/route.ts` | Opportunities (update status) | User (admin click in Candidate Section) | `opportunities` | **Critical** | **Migration note:** Trivial mechanically (1 `pages.update`) but **critical** because it's the user-visible promotion action paired with #5. Must ship in the same PR as #5 to avoid a half-migrated read/write split. Updates `Opportunity Status` and `Follow-up Status` selects; map directly to `opportunities.status` and `opportunities.follow_up_status` columns. |
| 7 | `src/app/api/create-candidate/route.ts` | Opportunities (create) | User (`/admin` Inbox Triage "+ Opportunity" button) | `opportunities` | **Critical** | **Migration note:** 1 `pages.create`. Pair with #5/#6 in the same migration window. Inserts a row with status=New, follow-up=Needed, scope=CH from inbox context. Direct map to `opportunities` columns; preserve `legacy_notion_id` as null for new records. |
| 8 | `src/app/api/extract-meeting-evidence/route.ts` | Evidence (create) | Cron (`0 2 * * 2-6`) + manual via CRON_SECRET | `evidence` | **Critical** | **Migration note:** 1 `pages.create` per extracted evidence record. Today writes Validation Status=New for `validation-operator` to pick up. After migration, `evidence` rows in Supabase must include `validation_status` so the validation-operator (also being migrated, see #11) reads/writes it natively. Watch for the project-link relation (`Affected Project`) — needs FK to `projects.notion_id` or new `projects.id`. **[migrated 2026-05-05]** `writeEvidence` now inserts canonically into `evidence` (title, evidence_type, evidence_statement, source_excerpt, validation_status, confidence_level, sensitivity_level, legacy_source_db, date_captured, affected_theme, geography, topics, org_notion_id, project_notion_id). Notion call commented as `notion-cutoff-2026-06-02` marker. Dedup pre-loader (`loadExistingEvidenceKeys`) reads Supabase first with Notion fallback. |
| 9 | `src/app/api/extract-conversation-evidence/route.ts` | Evidence (create), Sources (update `evidence_extracted` flag) | User (fire-and-forget from `/api/clipper`) + cron sweep | `evidence`, `sources` | **Critical** | **Migration note:** 2 write sites. Same pattern as #8 but sourced from Supabase (`sources` + `conversation_messages`). Already reads Supabase. The output write to Notion is the only remaining leg. Maintaining the `sources.evidence_extracted` flag across both Supabase and Notion is currently double-bookkept; collapse to Supabase only. **[migrated 2026-05-05]** `writeEvidence` inserts into `evidence` with `source_notion_id` traceback. `markSourceExtracted` collapses to a single `sources.evidence_extracted=true` Supabase update; the Notion `Evidence Extracted?` checkbox write is removed. Dedup pre-loader reads Supabase first with Notion fallback. |
| 10 | `src/app/api/generate-daily-briefing/route.ts` | Daily Briefings (upsert one record per day) | Cron (`30 7 * * 1-5`) | `daily_briefings` (canonical, replacing `notion_daily_briefings` mirror) | **Critical** | **Migration note:** 1 `pages.update` (existing) + 1 `pages.create` (new). The Hall reads this on every page load via the mirror. Migration must (a) write to `daily_briefings` table, (b) repoint Hall reader (`getRecentDailyBriefing` or similar) to the canonical table, (c) drop the mirror push-pending path. Single-record-per-day upsert key is the date string. **[migrated 2026-05-05]** Both `pages.update` and `pages.create` now route through a single Supabase upsert keyed on `briefing_date`. Sections concatenate into `body_md`; structured payload (`payload.sections`, `payload.generated_at`, `payload.status`) preserves the 7 named sections until Phase 6 binds dedicated columns. Notion existence check kept as a fallback observability hint until cutoff. Hall reader migration is a separate follow-up. |
| 11 | `src/app/api/validation-operator/route.ts` | Evidence (update Validation Status, archive) | Cron (`0 3 * * 1-5`) + manual | `evidence` | Complex | Two-pass sweep: New→Validated/Reviewed, then Reviewed→Validated/Archived. Already reads from Supabase-server. Bulk update path needs an `UPDATE evidence SET validation_status=…` analog. |
| 12 | `src/app/api/project-operator/route.ts` | Projects (Status Summary + Draft Status Update) | Cron (`0 5 * * 1-5`) | `projects` (add `status_summary`, `draft_status_update` columns) | Complex | 1 `pages.update` per project where ≥2 new validated evidence items exist. Must move evidence read to Supabase + write status_summary to projects table. |
| 13 | `src/app/api/fireflies-sync/route.ts` | Projects (Last Meeting Date), Sources (create meeting source), People (Last Contact Date) | Cron (`30 6 * * 1-5`) | `projects`, `sources`, `people` | Complex | 1 `pages.update` + 1 `pages.create` for sources + 1 `pages.update` for people in a loop. All three target tables already exist in Supabase. Largely a write-path swap with stable schema. **[migrated 2026-05-05]** Project `Last Meeting Date` now writes `projects.last_meeting_date` keyed on `notion_id`. Source meeting create now inserts into `sources` (title, source_type=Meeting, source_platform=Fireflies, processing_status=Processed, source_date, source_url, project_notion_id, processed_summary, dedup_key=`fireflies:<id>`, source_external_id). People last-contact path was already Supabase-only. Dedup pre-loader (`getExistingSourceUrls`) reads `sources` first with Notion fallback. |
| 14 | `src/app/api/ingest-meetings/route.ts` | Agent Drafts (create), People (update Last Contact Date) | Cron (`0 18` and `0 0` weekday) | `agent_drafts` (replacing mirror), `people` | Complex | 1 `pages.update` + 1 `pages.create`. Already reads people from Supabase. Agent Drafts is a mirror-replace target. **[migrated 2026-05-05]** `writeAgentDraft` now inserts into `agent_drafts` (draft_type="Market Signal", status="Pending Review", title, body_md, source_agent="ingest-meetings", payload={source_reference, window, meeting_count, date}). `updatePeopleLastContact` collapses to a Supabase `people.last_contact_date` update; the Notion `Last Contact Date` write is removed. Notion DB query retained as a read-only fallback to resolve email→person before cutoff. |
| 15 | `src/lib/notion-push.ts` | Sources (create), Evidence (create batch), Knowledge Assets (create), Sources (append blocks for audit body) | Programmatic (called from `/api/ingest-library/digest/execute`) | `sources`, `evidence`, `knowledge_assets` | Complex | 4 `pages.create` + 1 `blocks.children.append`. Phase C of the digest pipeline — full proposal push including bidirectional Evidence↔KA backlinks and audit summary. Knowledge Assets table is **NEW**; Phase 1 dependency. |
| 16 | `src/app/api/ingest-gmail/route.ts` | Sources (create) | Cron (`0 7 * * 1-5`) | `sources` | Moderate | 1 `pages.create` per new thread. Direct field mapping. Need to ensure `sources.url` uniqueness to keep the Gmail-thread dedup contract. **[migrated 2026-05-05]** New thread inserts now go straight to `sources` (title, source_type=Email, source_platform=Gmail, source_url, processing_status=Ingested, source_date, dedup_key=`gmail:<threadId>`, thread_id, project_notion_id). Notion `pages.create` commented as `notion-cutoff-2026-06-02` marker. `threadAlreadyIngested` reads `sources.dedup_key`/`thread_id` first; Notion query retained as fallback until backfill catches up. |
| 17 | `src/app/api/ingest-library/route.ts` | Knowledge Assets (create via `createKnowledgeAssetDraft`), Library docs (archive on delete) | User (admin upload) | `knowledge_assets` (NEW) | Moderate | Calls `createKnowledgeAssetDraft` from `src/lib/notion/knowledge.ts`. Also archives Notion pages on delete (`pages.update {archived:true}`). Both paths swap to `knowledge_assets`. **[migrated 2026-05-05]** POST handler now inserts directly into `knowledge_assets` (title, asset_type, status="Draft", summary, body_md composed from summary + key points + source note, payload={tags, source_file_url, source_note, storage_path, portal_visibility, is_external, source_agent}). DELETE handler updates `knowledge_assets.status="Archived"` (matching by uuid `id` first, then legacy `notion_id`). The `createKnowledgeAssetDraft` import is removed; the helper in `src/lib/notion/knowledge.ts` is now unused by this route and slated for deletion at Phase 6. |
| 18 | `src/app/api/relationship-warmth/route.ts` | People (Contact Warmth, Last Contact Date) | Cron (`0 6 * * 1,4`) | `people` | Moderate | 1 `pages.update` per changed person. Already reads Supabase-first. Trivial field mapping. |
| 19 | `src/app/api/grant-radar/route.ts` | Opportunities (create grant records) | Cron (`0 7 * * 3`) + admin manual | `opportunities` (with type=Grant) + `grant_sources` (NEW) for source list | Moderate | 1 `pages.create` per proposed grant. Must coordinate with `grant_sources` table and the grants page reader. |
| 20 | `src/app/api/evidence-to-knowledge/route.ts` | Knowledge Assets (create draft) | Cron (`0 4 * * 1-5`) | `knowledge_assets` (NEW) | Moderate | 1 `pages.create`. Synthesizes from canonical evidence; conservative draft-only. Maps cleanly. |
| 21 | `src/app/api/run-skill/delegate-to-desk/route.ts` | Agent Drafts (create) | User (admin "Delegate" action) | `agent_drafts` | Moderate | 1 `pages.create`. Already reads assignee from Supabase-first. |
| 22 | `src/app/api/run-skill/draft-checkin/route.ts` | Agent Drafts (create) | User (admin) | `agent_drafts` | Moderate | 1 `pages.create`. Already reads person from Supabase-first. |
| 23 | `src/app/api/run-skill/draft-followup/route.ts` | Agent Drafts (create) | User (admin) | `agent_drafts` | Moderate | 1 `pages.create`. Reads opportunity from Notion — should switch to `opportunities` table. |
| 24 | `src/app/api/run-skill/identify-quick-win/route.ts` | Agent Drafts (create) | User (admin) | `agent_drafts` | Moderate | 1 `pages.create`. Mixed read sources (Supabase opportunities, Notion decisions/content/people). |
| 25 | `src/app/api/run-skill/linkedin-post/route.ts` | Agent Drafts (create) | User (admin) | `agent_drafts` | Moderate | 1 `pages.create`. Reads insight briefs + knowledge from Notion — read paths also migrate. |
| 26 | `src/app/api/propose-content-pitches/route.ts` | (No direct write call site; reads Insight Briefs) | Cron (`0 9 * * 5`, last Friday gate) | n/a (writes happen via `comms-strategy` lib to `comms_*` Supabase tables) | Trivial | Imports `@notionhq/client` + queries Insight Briefs DB only. **No write call site.** Migrate read path to `insight_briefs`. |
| 27 | `src/lib/generate-draft.ts` | Content Pipeline (databases.update to add Slide HTML property; pages.update with Draft Text + Slide HTML) | Programmatic (called by content-generation flows) | `content_pipeline_items` (canonical, replacing mirror) | Moderate | 1 `databases.update` (one-time schema mutation) + 2 `pages.update`. The `databases.update` to ensure Slide HTML property goes away entirely once Supabase schema is fixed in Phase 1. |
| 28 | `src/app/api/admin/approve-project-update/route.ts` | Projects (Status Summary, Draft Status Update, Project Update Needed?) | User (admin "Approve project update" banner) | `projects` | Moderate | 1 `pages.update`. Direct field mapping; pair with #12 (`project-operator`) so write target is consistent. |
| 29 | `src/app/api/approve-draft/route.ts` | Agent Drafts (status: Approved/Revision Requested) | User (admin Outbox approve/revision button) | `agent_drafts` | Trivial | 1 `pages.update`. Status select swap. **[migrated 2026-05-05]** Status now written to `agent_drafts` keyed on `notion_id`; Notion call commented with `notion-cutoff-2026-06-02` marker. |
| 30 | `src/app/api/update-draft/route.ts` | Agent Drafts (Content rich_text) | User (admin Outbox inline edit) | `agent_drafts` | Trivial | 1 `pages.update`. **[migrated 2026-05-05]** Body now written to `agent_drafts.body_md` keyed on `notion_id`. |
| 31 | `src/app/api/send-draft/route.ts` | Agent Drafts (status update after Gmail send) | User (admin Outbox send) | `agent_drafts` | Trivial | 1 `pages.update`. **[migrated 2026-05-05]** Status now written to `agent_drafts` keyed on `notion_id`. Notion `pages.retrieve` (read) retained for body fetch — separate read-path migration. |
| 32 | `src/app/api/assign-draft-contact/route.ts` | Agent Drafts (Related Entity relation), Opportunities (Key Contacts relation if applicable) | User (admin) | `agent_drafts`, `opportunities` | Moderate | 2 `pages.update` (draft + optional opportunity). Person resolution already Supabase-first. |
| 33 | `src/app/api/flag-opportunity/route.ts` | Opportunities (Follow-up Status, Trigger/Signal note) | User (admin Opportunities Explorer flag) | `opportunities` | Trivial | 1 `pages.update`. |
| 34 | `src/app/api/followup-status/route.ts` | Opportunities (Follow-up Status) | User (admin Chief-of-Staff Desk) | `opportunities` (+ `loops` table side-effect already Supabase) | Trivial | 1 `pages.update`. Loop-engine side-effect already writes Supabase. |
| 35 | `src/app/api/content/[id]/route.ts` | Content Pipeline (Status: Archived or arbitrary) | User (admin content card) | `content_pipeline_items` | Trivial | 2 `pages.update` (archive vs status). **[migrated 2026-05-05]** Both PATCH branches now update `content_pipeline_items` keyed on `notion_id` (legacy URL segment) or `id` (uuid). Notion calls commented as `notion-cutoff-2026-06-02` markers. |
| 36 | `src/app/api/desk-request/route.ts` | Content Pipeline (create new design/comms desk request) | User (admin desk forms) | `content_pipeline_items` | Trivial | 1 `pages.create`. **[migrated 2026-05-05]** Insert now writes to `content_pipeline_items` with title/status/channel/body_md columns; Content Type / Desk / Project / Style Profile go to `payload` until Phase 6 binds columns. |
| 37 | `src/app/api/offers-create/route.ts` | Offers (create) OR Proposal Briefs (create) depending on docType | User (admin Offers page) | `offers` (NEW), `proposal_briefs` (NEW) | Moderate | 2 `pages.create` branches. Both targets are NEW Supabase tables. |
| 38 | `src/app/api/garage-upload/finalize/route.ts` | Data Room (create document record) | User (admin Garage upload) | `data_room_documents` (NEW) | Moderate | 1 `pages.create`. Already reads project page from Notion — also migrates. |
| 39 | `src/app/api/garage-upload/route.ts` | None (storage signed URL) — also `pages.update {archived:true}` for delete | User | `data_room_documents` | Trivial | 1 `pages.update` archive on delete. Replace with row delete in `data_room_documents`. |
| 40 | `src/app/api/upload/route.ts` | Sources (create source for client portal upload) | User (resident or admin upload) | `sources` | Trivial | 1 `pages.create`. |
| 41 | `src/app/api/hall-organizations/sync-notion/route.ts` | Organizations (create or refresh) | User (admin "sync-notion" button on Hall org detail) | `organizations` | Moderate | 1 `pages.create`. **Per freeze §3.7 this entire route is to be deleted.** |
| 42 | `src/app/api/sync-organizations/route.ts` | None (read-only Notion → Supabase) | Cron (`0 12 * * 1-5`) | n/a — already writes to `organizations` | Trivial | Read-only sync. **Per freeze §3.7 the route is deleted at cutoff** (sync direction reverses). |
| 43 | `src/app/api/sync-projects/route.ts` | None (read-only) | Cron | n/a | Trivial | Read-only sync; delete at cutoff. |
| 44 | `src/app/api/sync-people/route.ts` | None (read-only) | Cron | n/a | Trivial | Read-only sync; delete at cutoff. |
| 45 | `src/app/api/sync-evidence/route.ts` | None (read-only) | Cron | n/a | Trivial | Read-only sync; delete at cutoff. |
| 46 | `src/app/api/sync-sources/route.ts` | None (read-only) | Cron | n/a | Trivial | Read-only sync; delete at cutoff. |
| 47 | `src/app/api/sync-opportunities/route.ts` | None (read-only) | Cron | n/a | Trivial | Read-only sync; delete at cutoff. |
| 48 | `src/app/api/sync-loops/route.ts` | None to Notion (writes to Supabase loops) | Cron | n/a | Trivial | Reads three Notion DBs → writes Supabase loops. After cutoff reads switch to Supabase tables; route may be retained or deleted depending on whether loop-engine logic stays. |
| 49 | `src/app/api/mark-grant-interest/route.ts` | Opportunities (create or update grant interest) | User (admin grants page) | `opportunities` | Moderate | 1 `pages.update` + 1 `pages.create` branch. |
| 50 | `src/app/api/seed-grant-sources/route.ts` | Grant Sources (one-shot create per item) | One-shot admin seed | `grant_sources` (NEW) | Trivial | 1 `pages.create` in a loop. Run once on Supabase side then delete the route. |
| 51 | `src/app/admin/os/actions.ts` | Evidence (Validation Status: Reviewed; batch) | User (admin OS page) | `evidence` | Trivial | 3 `pages.update` sites (`markEvidenceReviewed`, `batchMarkReviewed`, plus a third). |
| 52 | `src/lib/notion-mirror-push.ts` | decision_items / daily_briefings / competitive_intel mirrors → push pending changes back to Notion | Cron (`30 6 * * *`) via `/api/cron/push-pending-to-notion` | n/a — **delete entirely at cutoff** | Complex | 1 `pages.update` per pending mirror row. Per freeze §3.7 this whole file goes away. The mirror tables drop too, so all callers (`/api/resolve-decision`, etc.) collapse to direct canonical-table writes. |
| 53 | `src/lib/notion/knowledge.ts` | Knowledge Assets (create) | Programmatic (`createKnowledgeAssetDraft` helper) | `knowledge_assets` (NEW) | Trivial | 1 `pages.create`. Replaced by a Supabase insert helper. |
| 54 | `src/lib/notion/living-room.ts` | People (`relationship_class`, `relationship_classes`, `engagement_type`, `engagement_value` updates) | Programmatic (called from Living Room organization-promotion flows) | `organizations` (add `relationship_classes text[]`, `engagement_type`, `engagement_value`) | Moderate | 4 `pages.update` sites — these are the live writes for the Living-Room → Engagements / engagement classification flow. Targets organizations table per freeze §3.1. |

### Section A summary

- **Files imported `@notionhq/client`:** 37 (per `grep -rln "@notionhq/client" src/`).
- **Files containing actual Notion *write* call sites:** 54 entries above (call-site-bearing files; some files have multiple call sites).
- **Critical migrations** (called out by name in the task brief):
  - `decisions-queue` → already **read-only** (`GET`), no write to migrate; only its read source moves to `decision_items`.
  - `approve-draft`, `extract-meeting-evidence`, `extract-conversation-evidence`, `promote-candidate`, `create-candidate`, `generate-daily-briefing` — all itemised above with multi-sentence migration notes.
- **Cron-triggered writers** (10): competitive-monitor, extract-meeting-evidence, extract-conversation-evidence, fireflies-sync, generate-daily-briefing, grant-radar, ingest-gmail, ingest-meetings, project-operator, propose-content-pitches, relationship-warmth, validation-operator, push-pending-to-notion (cron wrapper), evidence-to-knowledge.
- **User-triggered writers** (~30): all `/api/run-skill/*`, candidate/promote/flag/followup-status, content/desk-request, garage-* upload+ingest, offers-create, mark-grant-interest, send-draft/update-draft/assign-draft-contact, upload, admin/approve-project-update, hall-organizations/sync-notion, plus the two server-action files.
- **Read-only sync routes** (deleted at cutoff): sync-organizations, sync-projects, sync-people, sync-evidence, sync-sources, sync-opportunities, sync-loops.
- **Routes I could not classify cleanly:**
  - `propose-content-pitches` imports `@notionhq/client` but the only Notion call is a *read* against Insight Briefs; listed at row 26 as "no write call site". After migration, even the read goes to `insight_briefs`.

---

## Section B — Phase 5: UI gap fill

For each Notion DB in `src/lib/notion/core.ts` `DB` constant, the table below records whether the portal already has an editor surface and what the minimum CRUD page should be if not.

| Notion DB (constant key) | Has portal edit UI today? | Existing admin page(s) | Minimum CRUD needed | Priority |
|---|---|---|---|---|
| `projects` (CH Projects [OS v2]) | **Partial** — read-only project detail + flag-banner approve-update | `/admin/projects/[id]/page.tsx`, `/admin/garage/[id]/page.tsx` (portfolio sub-view) | List (already exists implicitly via Hall + Garage), detail edit form for: Status, Stage, Status Summary, Project Update Needed?, Themes, Geography, Primary Org relation | **P0** — needed because freeze §4 explicitly lists "Project status edit" as a P0 gap |
| `evidence` (CH Evidence [OS v2]) | **No** — `/admin/os` page exists but only does "mark Reviewed" batch button. No record edit. | `/admin/os/page.tsx` (queue view + actions), no detail page | List (already on `/admin/os`), detail page with edit form for: Title, Type, Statement, Confidence, Validation Status, Affected Theme, Topics, Source Excerpt, project/org relations | **P0** — explicitly called out in freeze §4 as "Build minimal CRUD" |
| `sources` (CH Sources [OS v2]) | **No** | none | List + detail with edit for: Title, Source Type, Platform, URL, Date Captured, Relevance Status, Project/Org relations | **P2** — most writes are agent-driven (Gmail/Fireflies/Clipper); manual edits are rare. Append-only-via-API acceptable. |
| `knowledge` (CH Knowledge Assets [OS v2]) | **Partial** — read-mostly explorer (`/admin/knowledge`, `/admin/knowledge/[...path]`, `/admin/knowledge/cases/[code]`, `/admin/knowledge/reading-room`); add-form at `/admin/knowledge/add` | `/admin/knowledge/*` (multiple pages) | Extend existing `add` page into full edit form. Fields: Asset Name, Asset Type, Domain/Theme, Status, Portal Visibility, Source File URL | **P1** — freeze §4 lists it as "Extend `/admin/knowledge`" |
| `people` (CH People [OS v2]) | **Partial** — `/admin/hall/contacts`, `/admin/hall/contacts/[email]`, `/admin/hall/network` — currently mostly read with relationship-class / tag editing on contact detail | `/admin/hall/contacts/*` | Extend contact detail form to cover: Full Name, Email, Job Title, Relationship Class(es), Internal Owner, Notes, LinkedIn — mostly already there | **P1** — covered by existing pages, just needs contract pinning |
| `decisions` (CH Decision Items [OS v2]) | **Partial** — `/admin/decisions` covers Open queue, status changes, relation resolution, entity creation flows. Missing: editing decision title/body/priority. | `/admin/decisions/page.tsx` + `actions.ts` | Extend with edit form (title, priority, due date, related entity) and "create new decision" path | **P0** — freeze §4 says "Extend to cover all decision types" |
| `insightBriefs` (CH Insight Briefs [OS v2]) | **No** | none directly; consumed inside `/admin/insights/page.tsx` (read-only?) | List + detail with edit for: Title, Status, Body, Source relation, Theme | **P2** — agent-written; append-only acceptable. Keep an admin-only "view+archive" minimal page. |
| `contentPipeline` (CH Content Pipeline [OS v2]) | **Yes** — `/admin/comms/page.tsx` and `/admin/content/page.tsx` already cover this with edit/archive/send actions; `/admin/plan/comms/page.tsx` for pitch planning. **Reconcile with `comms_*` tables** per freeze §4 note. | `/admin/comms`, `/admin/content`, `/admin/plan/comms` | No new page; reconcile naming so the comms/content read+write path lands on `content_pipeline_items` post-cutoff | **P1** — naming-only reconciliation, no UI lift |
| `styleProfiles` (CH Style Profiles [OS v2]) | **No** — read-only consumed in comms pages | none | List + detail edit for: Name, Tone, Examples, Persona | **P2** — rarely edited; append-only acceptable |
| `organizations` (CH Organizations [OS v2]) | **Partial** — `/admin/hall/organizations/page.tsx` (list) and `/admin/hall/organizations/[domain]/page.tsx` (detail). Currently tag-only editing per freeze §4 note. | `/admin/hall/organizations/*` | Extend detail page with edit form for: Name, Website, Relationship Stage, Organization Category, Themes, Engagement Type, Engagement Value, Relationship Classes, Internal Owner | **P0** — freeze §4 lists "Organization full edit" with "Extend `/admin/hall/organizations/[domain]`" |
| `valuations` (CH Valuations [OS v2]) | **No** | consumed in `/admin/garage/[id]` portfolio view (read-only) | List + detail edit for: Period, Pre-money, Post-money, Round, Org relation | **P2** — append-only acceptable; primary writes are from `garage-ingest` |
| `capTable` (CH Cap Table [OS v2]) | **No** | read-only inside `/admin/garage/[id]` | List + detail edit per holder: Holder, Class, Shares, % | **P2** — same as valuations |
| `dataRoom` (CH Data Room [OS v2]) | **Partial** — upload UI exists (`/admin/garage/[id]` + `garage-upload/finalize`); read-only listing in detail page | `/admin/garage/[id]` | Extend with rename/archive controls; list view standalone optional | **P1** — already mostly built; minor lift |
| `financialSnapshots` (CH Financial Snapshots [OS v2]) | **No** | read inside `/admin/garage/[id]` financial summary | List + detail edit for: Period, Revenue, Burn, Cash, Runway, Margin, ARR, MRR, scope relation | **P2** — agent-written via `garage-ingest`; manual edits rare |
| `proposalBriefs` (CH Proposal Briefs [OS v2]) | **Partial** — `/admin/offers/page.tsx` creates them (offers-create), no list/detail | `/admin/offers/page.tsx` | List + detail edit for: Title, Status, Proposal Type, Budget Range, Body | **P1** — needed to ship the proposal lifecycle in-portal |
| `offers` (CH Offers [OS v2]) | **Partial** — `/admin/offers/page.tsx` creates them, list+detail editing missing | `/admin/offers/page.tsx` | List + detail edit for: Offer Name, Status, Notes, Value, Org relation | **P1** — needed for offer lifecycle |
| `opportunities` (CH Opportunities [OS v2]) | **Partial** — `/admin/opportunities/page.tsx` (Explorer, read+flag), `/admin/pipeline/page.tsx`, `/admin/deal-flow/page.tsx`, plus `/admin` Candidate Section | `/admin/opportunities`, `/admin/pipeline`, `/admin/deal-flow` | Extend with full edit form: Name, Status, Type, Priority, Trigger/Signal, Source URL, Account, Key Contacts, Follow-up Status | **P0** — heavy daily-write surface; needed by `promote-candidate` migration |
| `grantSources` (CH Grant Sources [OS v2]) | **No** | none (used by grant-radar) | List + detail edit for: Name, Type, Geo, Themes, URL | **P2** — seeded once; rare edits |
| `agentDrafts` (CH Agent Drafts [OS v2]) | **Yes** — Outbox UI on `/admin` and per-section components: approve, revise, send, update content, assign contact | `/admin/page.tsx` (Outbox section) + multiple `/api/run-skill/*` and `/api/{approve,update,send,assign}-draft` | No new page; existing Outbox covers create-by-agent + admin review/approve/send. Migrate read+write target to `agent_drafts`. | **P1** — naming/contract reconciliation only |
| `dailyBriefings` (CH Daily Briefings [OS v2]) | **No** | consumed by Hall (`getRecentDailyBriefing`) | None — append-only-via-API acceptable per freeze §4 | **P2** |
| `watchlist` (CH Watchlist [OS v2]) | **No** | consumed by `competitive-monitor` and Hall panels | List + add/remove form for: Name, Type (Competitor/Sector/Partner/Referente/Cliente Potencial), URL, Notes | **P1** — freeze §4 lists "Build in Hall" |
| `competitiveIntel` (CH Competitive Intel [OS v2]) | **Yes** — `/admin/competitive-intel/page.tsx` + `CompetitiveIntelClient.tsx` allow read + relevance/status edits via mirror push | `/admin/competitive-intel` | No new page; reconcile target table | **P1** — repointing only |

(Two DBs from the freeze §3 list are not in `core.ts` `DB` constant: **CH Conversations** — already Supabase-native via `conversation_messages`, no Notion DB; **Engagements / CH Startup Relationships** — exists in Notion but not exposed via the `DB` constant. Engagements has no portal UI; freeze §4 says "Build new section under `/admin/clients`".)

### Build queue — ordered list of UI pages to build (P0 → P2)

Effort estimates: **S** = ≤1 day, **M** = 2–3 days, **L** = 4+ days. Order is execution order, dependencies first.

#### P0 — must ship before Phase 6 (2026-06-02)

1. **Decisions full edit / create page** — extend `/admin/decisions` with title/priority/due-date edit + "new decision" form. **M**. Unblocks the decisions write-path migration.
2. **Project status edit form** — `/admin/projects/[id]` add edit panel for Status, Stage, Status Summary, Project Update Needed?. **M**. Unblocks `project-operator` and `approve-project-update`.
3. **Organization full edit** — extend `/admin/hall/organizations/[domain]` with full property editor (per fields above). **M**. Unblocks org-creation flows in `decisions/actions.ts` and `garage-ingest`.
4. **Opportunity full edit** — promote `/admin/opportunities` detail to full editor; cover all status/follow-up/priority/relations. **L**. Unblocks `promote-candidate`, `create-candidate`, `flag-opportunity`, `followup-status`, `mark-grant-interest`.
5. **Evidence detail+edit page** — new `/admin/os/[id]/page.tsx` with full evidence record editor. **M**. Unblocks evidence-write paths and supports validation operator overrides.

#### P1 — must ship by 2026-05-30 (freeze §4 cutoff)

6. **Engagements section** under `/admin/clients` — list + create + edit form. **M**. Required by `relationship-promotion-operator` (freeze acceptance criterion #6).
7. **Watchlist add/remove panel in Hall** — minimal CRUD on a single Hall section. **S**. Required for competitive-monitor inputs.
8. **Knowledge Assets edit** — extend `/admin/knowledge/add` into a unified edit form usable from any KA detail. **M**.
9. **Offers / Proposal Briefs list + detail** — extend `/admin/offers` with list + detail editor for both target tables. **M**.
10. **People (contacts) edit reconciliation** — pin `/admin/hall/contacts/[email]` to write to `people` directly post-cutoff. **S**.
11. **Data Room rename/archive controls** — small additions to `/admin/garage/[id]`. **S**.
12. **Content Pipeline / Agent Drafts / Competitive Intel naming reconciliation** — point existing pages at canonical Supabase tables, no new UI. **S** each (3 × S).

#### P2 — append-only-via-API acceptable; can land after cutoff

13. Sources detail edit (admin-only) — **S**.
14. Insight Briefs view + archive — **S**.
15. Style Profiles list + edit — **S**.
16. Valuations / Cap Table / Financial Snapshots edit — **M** combined.
17. Grant Sources admin list — **S**.
18. Daily Briefings view (read-only is fine) — **S**.

---

**Total Build Queue:** 5 P0 (1×L + 4×M ≈ 12 days), 7 P1 items (1×L + 4×M + 2×S ≈ 13 days), 6 P2 items (~1 week). With one engineer this exceeds the 2026-05-30 budget; freeze §4 explicitly authorises cutting P2 to append-only-via-API.
